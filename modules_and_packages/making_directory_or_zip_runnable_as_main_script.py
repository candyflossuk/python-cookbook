"""
This script contains information on how to run a program consisting of multiple scripts by
adding a __main__.py file and running the app (which will pick up main.py) and run it.

add a __main__.py at the top level of the application

run using:
python3 myapplication
Where myapplication is the name of the top level directory

This also works if you package up your code into a zip as follows
zip -r myapp.zip *.py
python3 myapp.zip
"""
